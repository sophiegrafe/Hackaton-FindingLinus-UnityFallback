mergeInto(LibraryManager.library, {
  // HelloString: function (str) {
  //   window.alert(Pointer_stringify(str));
  // },

  // GameMessage: function (evtsrc,evtval) {
  //   window.GameMessage(Pointer_stringify(evtsrc),Pointer_stringify(evtval));
  // },

  showEnigma: function (objectName) {
    window.showEnigma(Pointer_stringify(objectName));
  },
});